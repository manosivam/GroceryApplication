package org.grocery.security;

/**
 * @author Manosivam
 *
 */
//making it package specific. (default modifier)
interface ICredentialsManager {

	bool authenticateCredential(Credentials credentials);
	
}
